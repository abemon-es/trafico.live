[build]
builder = "dockerfile"
dockerfilePath = "services/incident-collector/Dockerfile"
watchPatterns = ["services/incident-collector/**", "prisma/schema.prisma"]

[deploy]
cronSchedule = "*/5 * * * *"
numReplicas = 1
restartPolicyType = "NEVER"
