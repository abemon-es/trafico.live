[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Temporary: run once now, then revert to regular schedule
# Regular: cronSchedule = "0 6,13,20 * * *"
cronSchedule = "*/1 * * * *"
restartPolicyType = "NEVER"
