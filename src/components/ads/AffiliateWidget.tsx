// Ads temporarily deactivated — restore from git history when ready

export interface AffiliateWidgetProps {
  type: "insurance" | "fuel-card" | "ev-charger" | "itv";
  className?: string;
}

export function AffiliateWidget(_props: AffiliateWidgetProps) {
  return null;
}
